# ============================================================
#  Python Dependencies
#  Install with: pip install -r requirements.txt
# ============================================================

# Web framework — async, fast, modern
fastapi==0.111.0

# ASGI server — runs FastAPI
uvicorn[standard]==0.29.0

# PostgreSQL async driver
asyncpg==0.29.0

# Redis async client — for Pub/Sub
redis==5.0.4

# JWT authentication
python-jose[cryptography]==3.3.0

# Password hashing
passlib[bcrypt]==1.7.4

# Environment variable management
python-dotenv==1.0.1

# Data validation (bundled with FastAPI but pinned explicitly)
pydantic==2.7.1

# HTTP client for internal service calls (optional utility)
httpx==0.27.0
